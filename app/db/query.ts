// import { connection } from './connection';

// export const query = <T>(queryString: string): Promise<T> => {
//   return new Promise((resolve, reject) => {
//     connection.query(queryString, (err, results) => {
//       if (err) return reject(err);

//       resolve(results);
//     });
//   });
// };
